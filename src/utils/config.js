//TODO: add firebase configuration and export it
export const firebaseConfig = {
  //write your api config of firebase
};

//image configuration
export const imageConfig = {
  quality: 0.2,
  maxWidth: 800,
  maxHeight: 600,
  autoRotate: true,
};
